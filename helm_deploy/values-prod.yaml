---
# Per environment values which override defaults in hmpps-prisoner-pay-ui/values.yaml

generic-service:
  ingress:
    host: prisoner-pay.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "https://prisoner-pay.prison.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api.prison.service.justice.gov.uk"
    EXAMPLE_API_URL: 'https://template-kotlin.hmpps.service.justice.gov.uk'
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: hmpps-activities-management-prod
